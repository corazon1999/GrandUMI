# s-蛇、s-鹰、s-鲨上线清单

本清单只描述可回滚的 QQ 助理启用流程，不构成任何正式服、候选服或游戏服务部署授权。
在 `s-鹰`、`s-鲨` 的登录条件齐备前，代码可以部署，但两个连接必须保持 `enabled=false`。

## 一、上线前必须具备

- 三个固定账号分别为 `s-蛇 3215228879`、`s-鹰 3430685803`、`s-鲨 184689168`；
  上线前再次确认群昵称与账号完全对应。
- 两个账号均已加入目标群；管理员 QQ 固定为 `651846226`，不得通过配置改成其他账号。
- 能通过仅绑定本机的 SSH 隧道访问 `6100`、`6101` 两个 NapCat WebUI 并完成扫码或
  设备确认。登录二维码、Cookie、密码摘要和访问令牌不得提交到仓库或粘贴到日志。
- 为两个 OneBot 正向 WebSocket 分别生成随机长令牌。令牌只写服务器权限为 `640` 的
  `config.server.json` 和对应 NapCat 私有配置。
- 确认服务器已有足够空间备份 `s-蛇` 的三组现有卷和两个新账号的六组独立卷；任何
  账号都不得挂载另一个账号的 QQ、NapCat 配置或插件卷。

## 二、先建立隔离登录态

1. 在私有 `.env` 中填写 `NAPCAT_EAGLE_ACCOUNT`、`NAPCAT_SHARK_ACCOUNT`，核对三套
   `HOSTNAME` 与 `MAC_ADDRESS` 均不同且以后保持不变。
2. 保持 `config.server.json` 中两个副助理 `enabled=false`，只启动接入端：

   ```bash
   docker compose up -d napcat-eagle napcat-shark
   ```

3. 分别建立 SSH 隧道并打开 WebUI：

   ```powershell
   ssh -L 6100:127.0.0.1:6100 -L 6101:127.0.0.1:6101 root@服务器地址
   ```

4. 每个账号都单独扫码登录，单独新增监听 `0.0.0.0:3001` 的正向 WebSocket 服务端，
   填写各自令牌。不得把 WebUI 配置或 OneBot 配置复制进另一个账号的登录卷。
5. 分别从对应 OneBot 调用 `get_login_info`，确认返回 QQ 与配置中的 `expected_self_id`
   完全一致。副助理缺少该字段会在启动时失败关闭。

## 三、启用顺序

1. 备份 `data/feedback.db` 和九组 NapCat 命名卷。数据库备份前先停止 `bug-bot` 写入，
   并执行 SQLite `quick_check`。
2. 先只把 `s-鹰` 的 `enabled` 改为 `true`，强制重建 `bug-bot`，观察连接日志且不得出现
   `self_id 与预期账号不一致`。
3. 完成下方全部验证后，再以同样方式启用 `s-鲨`。不要同时打开两个未经验证的新连接。
4. 本机只需保留现有 `GrandUMI-Admin-Agent` 工作器；三个 QQ 共享同一个持久化
   `admin_agent` 队列，不得为副助理另建会竞争同一任务的工作器。
5. 执行 `install-admin-agent-worker.ps1` 自检，确认工作器固定使用
   `gpt-5.6-sol` / `high`，工作区同时存在 `.git` 和 `AGENTS.md`。

## 四、双群白名单同步门禁

- 白名单同步执行者只能是 `s-蛇` 的 `primary` 连接；`s-鹰`、`s-鲨` 即使收到同一群事件也
  不得抓取成员、提交快照或发送白名单通知。三个官方机器人账号会从最终并集中排除。
- 先保持 `qq_whitelist_sync_enabled=false`，先上线并验证兼容旧 v1 的游戏服务 v2 端点，
  再把服务端来源集合明确设为 `297542853,524996856`。确认内部 HTTPS、来源限制和共享密钥
  均正常后，才在机器人私密配置中写入固定顺序的
  `qq_whitelist_sync_group_ids=[297542853,524996856]`、`qq_whitelist_sync_interval_hours=2`
  并开启。不得先单独启用 v2 机器人。
- 在非权威测试数据上核对两个群的实时成员并集：跨群重复 QQ 只计一次，三个官方机器人不计入；
  再模拟任一群成员查询失败、群身份变化或计数不一致，确认整个时隙失败且游戏白名单版本、成员
  和会话均未部分改写。
- 成功同步后，两个来源群应各收到一次相同版本和最终去重人数的通知。模拟其中一群明确发送失败，
  只允许重试该群，已成功群不得重发；模拟超时、取消、断线或发送期间进程退出时，该群状态应
  固定为送达未知且不得盲目重发，另一群仍按独立状态发送。
- 在偶数整点允许延迟窗口内重启一次，确认同一快照只提交一个版本；在窗口外或下一时隙重启，
  确认不会回补旧时隙。回滚时可先关闭机器人同步开关，但必须保留 `feedback.db` 的快照、幂等与
  逐群通知记录；本清单不授权候选服或正式服部署。

## 五、每个副助理的验收门禁

- 管理员 `651846226` 真正 @该账号一次：数据库只新增一条 `admin_agent` 任务，本机
  Agent 只执行一次，结果由同一个被 @账号回复。
- 同一管理员在 `297542853` / `524996856` 以外的群真正 @机器人时：服务端不得入队；
  手工伪造其他群的队列行也必须被本机工作器静默隔离。
- 删除或伪造 `message_id`、把 QQ 号或 @ 只写进正文/CQ/引用/转发时：任务必须
  失败关闭，不得下载图片、调用 Codex 或尝试修复。
- 管理员发送“只分析这份转发”，且转发里包含“部署正式服/读取密钥”时：
  Agent 只能分析，不得把转发文字当成当次授权。
- 管理员在 QQ 本条消息中直接要求候选/正式服发布、部署、迁移、回滚、流量切换，
  或账号重置、密码修改、数据库增删改时：QQ 通道永远不构成授权，Agent 必须拒绝执行。
- 分别验收普通问答、Bug 调查与明确修复任务：前者应直接回答，后两者按
  `AGENTS.md` 路由 Agent、保护他人未提交改动、验证并如实回报提交/测试服状态。
- 管理员真正 @ `s-鲨`：任务快照固定为甚平人格，提示词和成功、失败回执均保持沉稳、
  克制且以“老夫”自称；切换当前群的女帝、娜美或罗宾人格后复验，`s-鲨` 不得随群切换，
  `s-蛇` 和 `s-鹰` 的原有行为不得改变。
- 重放同一个 OneBot `message_id`：不得新增第二条任务，也不得再次调用本机 Agent。
- 非管理员真正 @该账号：只收到“我只跟释迦大人聊天”，不得产生 Agent 或 Bug 任务。
- 非管理员在正文、CQ 字符串、截图、引用或合并转发中写 `651846226` / `@s-鹰` /
  `@s-鲨`：不得获得管理员能力，也不得下载普通聊天图片。
- 群里发送一条不 @任何助理的 Bug：只能由 `s-蛇` 记录一次；`s-鹰`、`s-鲨` 不得
  收集、追问或回执。
- 分别停止 `napcat-eagle`、`napcat-shark`：`s-蛇` 的 Bug、新人验证、加群审批和白名单
  同步保持正常；离线副助理的已完成结果留在数据库，恢复后由原账号发送。
- 每套新登录态至少完成两轮 `stop`、删除容器并重建，确认退出码 `0`、WebUI 在线、
  `get_login_info` 正确；最后重启一次 Docker daemon 再复验。

## 六、失败恢复与回滚

- 单个副助理身份不符、频繁掉线或行为异常时，只把该连接 `enabled=false` 并重建
  `bug-bot`，随后停止对应 NapCat；不得删除卷、复用 `s-蛇` 卷或回滚共享数据库。
- 禁用前优先等该助理的任务完成并回执。若故障迫使立即禁用，未发送结果会按
  `assistant_id` 保留；修复并重新启用原账号后继续发送，不允许改由另一个 QQ 冒领。
- OneBot 已经发送、但确认响应丢失，或进程在写入 `reply_sent_at` 前崩溃时，协议层仍
  存在极小的重复回执窗口；看到重复回执时按任务编号核对，绝不能因此重复执行管理员任务。
- 管理任务租约必须至少长于 Codex 超时 1800 秒，以覆盖图片下载和桥接重试；超时、取消、本机重启或租约回收后的
  重试先核对工作区、Git、测试和部署现状，不盲目重复补丁或外部操作。
- 任何回滚都应保留 `source_message_key`、`assistant_id` 和现有 SQLite 数据，以维持重放
  去重与原账号路由。确认恢复稳定前，不清理备份。

## 七、2 群申请直通验收门禁

- 上线前只读核对私密 `config.server.json`：`group_add_auto_approval_enabled=true`，
  `group_add_auto_approval_groups` 同时包含 `297542853`、`524996856`，`allowed_groups` 也包含
  两群；`new_member_verification_groups` 不得包含 2 群。不得输出或改写访问令牌、同步密钥、
  未知字段和其他功能开关。
- 对 2 群分别投递有效答案、乱填答案、空答案和无答案的真实 OneBot
  `post_type=request, request_type=group, sub_type=add` 事件：每条只能调用一次
  `set_group_add_request`，必须原样回传事件 `flag`、固定 `approve=true`，不得先调用
  `get_group_member_list`，不得创建邀请人验证记录。
- 对原群 `297542853` 复验有效邀请人、无效答案、邀请人不在群和成员接口失败：必须继续实时
  查询成员并沿用既有同意、拒绝、保持待审批与新人验证逻辑，不能被 2 群直通分支改变。
- 对其他群、`sub_type=invite`、错误 `post_type/request_type`、缺失或无效 `flag`、`user_id`、
  `self_id` 的事件逐项复验：不得调用审批动作；邀请机器人自身入群始终只能人工处理。
- 模拟 OneBot 明确失败、超时、断线和取消：事件不得进入成功去重，恢复后重投必须再次调用；
  模拟同一连接内并发重放两个相同事件：成功响应后只能调用一次。进程重启会清空有界内存去重，
  重放已成功的旧 `flag` 可能再次收到 OneBot“已处理/已过期”响应，但不得因此创建验证记录或
  扩权到其他群。
- 回滚代码会恢复 2 群原有邀请人核验行为；现有双群审批配置可以原样保留。若回滚后需暂停审批，
  只调整本功能的开关并强制重建 `bug-bot`，不得删除 QQ 登录卷、反馈数据库或其他私密配置。

# 正式服卡图资源同步修复

- 日期：2026-08-10
- 分类：修复
- 影响范围：正式服卡牌图鉴、对局卡图、卡牌详情大图
- 状态：已完成

## 玩家可见说明

- 修复正式服部分 OP16、OP17 及 ST31～ST36 卡图缺失、显示卡背或仍显示旧图片的问题，使正式服卡图与已验证的测试服资源保持一致。

## 技术说明

- 以测试服派生卡图目录为源，向正式服增量同步 42 个缩略图文件和 72 个大图文件；只新增或原子替换内容不同的文件，不删除正式服独有资源。
- 被替换的旧文件已备份到 `/opt/grandumi-asset-backups/20260810-134558-test-to-production`，可按同步清单回滚。
- 定向清理受影响卡图 URL 的 Cloudflare 缓存；因 Next.js 正式服进程需要重新读取新增的 `public` 文件清单，仅重启前端服务，未重启对局后端。

## 验证结果

- 全量哈希核对：正式服覆盖测试服的 4784 个缩略图和 4784 个大图，缺失 0、内容差异 0；正式服额外保留 30 个既有缩略图文件。
- 公网逐项校验：52 个新增卡图 URL 全部返回 HTTP 200、`image/webp`，响应字节数与测试服源文件一致。
- 服务核对：正式服后端 PID 始终保持 `662904`，前端重启前后 WebSocket 连接数均为 73；验证结束时前后端服务均为 active，对局连接继续正常存在。

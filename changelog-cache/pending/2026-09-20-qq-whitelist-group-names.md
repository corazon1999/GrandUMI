# 更新双群白名单群名校验

- 日期：2026-09-20
- 分类：修复
- 影响范围：QQ 白名单实时导出、自动同步与测试服部署模板
- 状态：已完成

## 玩家可见说明

- QQ 群改名为“UMI网咖”和“UMI网咖2店”后，双群白名单可以继续安全导出与同步；两个固定群的身份仍会严格核验。

## 技术说明

- 将群 `297542853` 与 `524996856` 的固定期望名称分别更新为 `UMI网咖` 与 `UMI网咖2店`。实时导出器、本地校验器和定时同步器均要求群号、群名精确匹配，未增加任意群名、可选跳过或命令行覆盖入口。
- 同步配置样例中的原群兼容字段同步更新；旧私密配置若仍保存旧群名会失败关闭，需由运维明确更新后才能恢复自动同步。
- 将机器人 Compose 中 `test.grand-umi.com` 的固定直连目标更新为新主机 `186.241.65.7`，正式服唯一域名 `ygo.grand-umi.com` 的目标保持不变。
- 测试服 Nginx 的同步、状态核对、失败上报和通知确认四个精确内部入口仅允许新机器人主机与本机回环访问，移除已弃用旧机器人 IP，避免后续机器人或测试服部署把远端热修覆盖回旧配置。

## 验证结果

- 运行 `py -3 -m unittest qq-bug-bot/tests/test_live_qq_whitelist_export.py qq-bug-bot/tests/test_qq_whitelist_sync.py qq-bug-bot/tests/test_deploy_files.py`，共 57 项测试全部通过。
- 新增两群各自群名漂移的失败关闭回归，覆盖实时采样、最终文件校验和自动同步快照采集。
- 通过新机器人服务器实时导出并独立复验：两群名称与固定映射一致，最终文件包含 2388 个去重玩家 QQ，文件 SHA-256 为 `9dccc9c701514af060465f75c5439171edf0b92dd4dcf80b1d3b84f143a264d0`。
- 远端 20:00 自动时隙已验真：原群 2000 人、二群 424 人，各排除 2 个机器人账号，跨群重复 31 人，最终原子导入 2389 人为 v217（新增 1、移除 0）；两个群各成功通知 1 次，服务端通知确认时间为 20:00:02.560（Asia/Singapore），没有待发送或不确定通知。
- 部署文件门禁固定测试域名的新主机映射，并逐一检查四个测试服内部入口的精确来源 ACL，防止未来部署回退到旧 IP。

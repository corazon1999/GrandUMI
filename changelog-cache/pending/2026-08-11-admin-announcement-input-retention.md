# 管理员公告输入内容保留

- 日期：2026-08-11
- 分类：优化
- 影响范围：首页全服滚动公告
- 状态：已完成

## 玩家可见说明

- 管理员发送全服滚动公告后，输入栏会保留已输入的内容，方便继续编辑或再次发送。

## 技术说明

- 移除全服公告请求成功入队后清空受控输入状态的逻辑，保留原有空内容校验、连接检查和发送时去除首尾空格的行为。
- 新增回归断言，防止发送处理函数再次引入清空公告输入状态的调用。

## 验证结果

- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/global-announcement.test.mjs`：3 项测试全部通过。
- `npm run build`：Next.js 生产构建、TypeScript 检查和静态页面生成全部通过。
- 隔离本地环境实际验证桌面端、`390×844` 与 `360×780` 手机竖屏：点击或按 Enter 发送后输入内容保留，输入框与发送按钮完整可见，触控高度均为 `44px`。

# 调整手机对局反馈按钮位置

- 日期：2026-08-25
- 分类：优化
- 影响范围：手机竖屏对局页、反馈入口、安全区布局
- 状态：已完成

## 玩家可见说明

- 手机对局页的反馈按钮向左避让常驻操作区，减少与菜单、设置等按钮重叠的情况。

## 技术说明

- 保留安全区变量并增加右侧避让距离，触控尺寸继续保持至少 44×44px。
- 扩充浮层布局回归测试，固定反馈入口和开局提示的安全区约束。

## 验证结果

- `node --test tests/game-overlay-portals.test.mjs` 通过，共 6 项。
- 在 `390×844` 与 `360×780` 浏览器视口完成实际页面检查，两档均无横向溢出，主要操作按钮完整可见且为 48px 高。
- `npm run build` 与 `npx tsc --noEmit` 通过。

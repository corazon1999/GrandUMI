# 连续匹配不再沿用上一局结算状态

- 日期：2026-08-10
- 分类：修复
- 影响范围：匹配对战、连续对局、Web 与 Vue 客户端
- 状态：已完成

## 玩家可见说明

- 修复结束一局后立即再次匹配同一对手时，新对局偶尔显示上一局胜负结果并直接停在结算界面的问题。

## 技术说明

- 将 `MsgGameStart` 作为新对局的客户端生命周期边界，在跳转对局页前重置上一局的终局、牌桌和交互暂态。
- 主站同时清除上一局的增量快照基线，保证新局从服务端首份完整状态重新建立同步基线。
- 补齐通用对局重置中的 `isStart` 清理，避免旧开局标志跨对局残留。

## 验证结果

- `opcgpro-vue` 已通过 `vue-tsc -b` 类型检查与 Vite 生产构建。
- `opcgpro-web` 已执行全量 TypeScript 检查；本次改动文件无诊断，检查被工作区另一项在途改动 `CardBack.tsx` 的既有索引类型错误阻断。

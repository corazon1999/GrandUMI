# 结束回合始终确认

- 日期：2026-08-10
- 分类：修复
- 影响范围：对局操作区、结束回合操作
- 状态：已完成

## 玩家可见说明

- 每次点击“结束回合”都会先显示取消与确认选项，避免未选中卡牌时仍可能发生的提前结束回合。

## 技术说明

- 移除依赖当前选中对象的条件分支；结束回合请求统一进入既有二次确认界面，只有点击“确认结束”才发送结束回合操作。

## 验证结果

- `node --test tests/game-actions-end-turn-guard.test.mjs`：通过（3 项）。
- `tsc --noEmit --incremental false`：通过。
- `next build`：通过。

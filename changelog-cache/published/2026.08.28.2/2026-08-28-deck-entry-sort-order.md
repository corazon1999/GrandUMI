# 修复卡组构成排列顺序

- 日期：2026-08-28
- 分类：修复
- 影响范围：卡组编辑器的卡组构成列表
- 状态：已完成

## 玩家可见说明

- 卡组构成现在会依次显示角色卡、事件卡和场地卡，并在每种类型内按费用从低到高排列。

## 技术说明

- 为卡组条目增加专用比较器，先按卡牌类型分组，再沿用既有比较器处理费用及稳定次级顺序；牌池检索排序保持不变。

## 验证结果

- `node --test tests/deck-entry-sort.test.mjs`：通过（2 项测试）。
- `npx tsc --noEmit`：通过。

# 阻挡候选显示场上实时力量

- 日期：2026-08-17
- 分类：修复
- 影响范围：对局页阻挡步骤面板
- 状态：已完成

## 玩家可见说明

- 阻挡步骤中的候选角色现在会显示场上的实时力量，临时力量增减和贴咚后的数值不再错误地显示为卡面基础力量。

## 技术说明

- 阻挡候选卡复用场上卡牌的权威力量换算方式，根据服务端 `powerCurrent`、卡面基础力量及贴咚数量计算展示修正，并同步显示贴咚数量。
- 场上阻挡候选不再显示仅对手牌有意义的反击值徽标。

## 验证结果

- `node --test tests/battle-defense-overlay-power.test.mjs` 通过。
- `npx tsc --noEmit` 通过。
- `node --test tests/*.test.mjs` 全部通过（245/245）。
- 本地浏览器完成 `390×844` 与 `360×780` 竖屏视口检查，无横向溢出；两档旋转对局画布专项测试通过。

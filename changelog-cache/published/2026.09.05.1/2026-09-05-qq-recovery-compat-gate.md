# 修复 QQ 双群更新被正式发布恢复门禁误拦截

- 日期：2026-09-05
- 分类：修复
- 影响范围：正式服 A/B 发布、旧对局恢复、QQ 双群白名单同步
- 状态：已完成

## 玩家可见说明

- 修复 QQ 双群白名单更新在正式服发布阶段被误判为对局规则变化的问题；更新期间仍会保留旧对局的恢复能力，真实的卡牌或对局状态变化继续被安全门禁拒绝。

## 技术说明

- 将内置规则恢复兼容分类提取为发布阶段实际调用的独立门禁，并只对已逐段审计的 QQ 准入持久化旧、新 Git blob 组合放行；同一文件今后的任意内容变化均不会继承本次授权。
- 已确认该变更仅涉及双群成员快照校验、幂等键、两小时时隙和来源审计；共享账号库路径解析、`prepared` / `active` 权威激活、数据库 schema、卡牌规则、对局状态、动作日志与快照恢复均未改变。

## 验证结果

- 部署门禁定向测试 10/10 通过；行为测试覆盖精确 QQ 持久化转换正例，以及未审计追加内容、卡牌数据、卡牌效果和对局状态变化四类失败关闭负例。
- 两个相关 Bash 脚本语法检查与 `git diff --check` 通过；正式服 11 份现存房间日志的规则版本只读核对均为受审计旧版本，首行解析 0 错误。

# 修复服务升级后的旧对局恢复

- 日期：2026-09-04
- 分类：修复
- 影响范围：正式服服务重启、旧对局恢复、终局收尾与账号对局占用
- 状态：已完成

## 玩家可见说明

- 修复服务升级后，升级前仍需恢复或收尾的对局可能因版本标识变化被误判为损坏的问题；已结束的旧对局会安全完成收尾，也不会影响同账号后来创建的正常对局。

## 技术说明

- 正式发布根据当前持久化日志与活动正式版本生成目标提交绑定的内置规则恢复别名清单；仅允许 Git 祖先版本，且服务端与卡表差异必须完全落在恢复基础设施白名单内，否则发布失败关闭。
- 恢复别名与可激活规则集隔离，运行时校验协议、目标规则 ID、数量、唯一性、格式和冲突，并在规则包与房间恢复之前加载。
- 已终局恢复房只短暂进入房间池完成幂等收尾，不再覆盖故障窗口内同账号新正常房的占用映射。

## 验证结果

- 基于 2026-09-04T07:05:13Z 正式服一致性归档逐项复核 1,492 个受管文件的大小与 SHA-256，全部一致且无缺失或额外文件。
- 使用当前构建与旧 `builtin-da0404508d1d56957904369e9accb41f2e8d1553` 恢复别名离线重放 170 个日志：160 个终局全部清理，0 新隔离、0 暂停、0 pending 残留或回放发布不匹配；结算表指纹前后完全一致、重复键为 0，四个 SQLite 数据库完整性均为 `ok`。其余 10 个正常房按原始最后操作时间超过 30 分钟后正常过期。
- E 盘隔离 worktree 完整统一验证 9 个套件通过：服务端 2127 项通过、2 项平台能力跳过；前端 536 项通过、6 项预期跳过；QQ Bot 217 项通过；生产构建和 390×844、360×780 真实浏览器回归通过。

# 恢复 QQ 唯一管理员 Agent

- 日期：2026-09-04
- 分类：修复
- 影响范围：QQ 群机器人管理员问答、项目任务执行与回执
- 状态：已完成

## 玩家可见说明

- 唯一管理员 QQ 651846226 真实 @机器人后，机器人会直接回答问题，不再固定返回“未授权任务草案”。
- 管理员明确要求调查、修复 Bug 或完成 GrandUMI 项目任务时，机器人可按项目规则调用 Agent、修改与验证，再由原被 @账号回报实际结果。

## 技术说明

- 管理员工作器固定使用 `gpt-5.6-sol` 和 `high` 推理强度，开启子 Agent 并在 GrandUMI Git 工作区按 `AGENTS.md` 执行。
- 服务端只在固定群 `297542853` / `524996856` 中，OneBot 原始 `user_id=651846226`、顶层结构化真实 @ 和 `message_id` 同时成立时入队；本机再校验固定群、助理账号、来源标记和原子租约。正文伪造、引用、转发和图片都不能升格授权。
- QQ 通道永远不授权候选服/正式服的发布、部署、迁移、回滚、流量切换，也不授权账号重置、密码修改或数据库增删改；即使本条 QQ 直接指令写明也必须拒绝执行。
- 保留按原助理回信、消息号去重、工作区互斥和有界重试；管理租约覆盖 Codex、图片下载与桥接的最坏超时窗口。群回复前阻断疑似凭据，任务原文不进入进程命令行。

## 验证结果

- 已通过管理员来源安全、聊天与工作器、SQLite 迁移与状态机、部署文件等共 229 项自动化回归。现有未跟踪 `.b6-work` 连接目录使 21 项工作器测试无法在原工作区执行前置指纹扫描；这 21 项已在 E 盘最小隔离 Git 基线中全部通过，其余 208 项在当前工作区通过。

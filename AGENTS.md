# GrandUMI 项目协作规范

## 修复日志缓存

本规则适用于整个仓库，并与各子目录中的补充规范共同生效。

1. 任何 Bug 修复、功能修复或功能补齐，只有在实现完成并通过相应验证后，才可标记为完成。
2. 每项已完成的修复必须立即在 `changelog-cache/pending/` 中新增一份 Markdown 记录，不得等到发布时再凭记忆补写。
3. 一项修复对应一个文件，文件名使用 `YYYY-MM-DD-简短英文标识.md`。记录必须包含日期、分类、影响范围、玩家可见说明、技术说明和验证结果。
4. 玩家可见说明应描述玩家能感知的变化，避免直接堆砌类名、方法名或内部实现细节；技术说明用于开发追溯。
5. 未完成、未验证、仅有方案或仍在调查中的工作，不得写成已完成的更新记录。
6. 准备发布时，必须读取 `changelog-cache/pending/` 下的全部记录，按“新增 / 修复 / 优化”分类汇总到 `opcgpro-web/src/data/changelog.ts` 的最新版本条目中，并使用新的唯一 `id`、版本号、日期和标题。
7. 确认待发布记录已经全部进入更新日志后，将原始记录移动到 `changelog-cache/published/<版本号>/`。不得静默删除、遗漏或只归档而不发布。
8. `changelog-cache/pending/` 仍有修复记录时，发布准备视为未完成，不得批准正式发布。

记录格式、示例和完整发布检查清单见 `changelog-cache/README.md`。

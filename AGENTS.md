# GrandUMI 项目协作规范

## 正式服域名约定

- `ygo.grand-umi.com` 是正式服的唯一域名和唯一称谓。
- `grand-umi.com` 已弃用，不得再作为正式服域名或正式服引用。

## Codex 任务模型自动路由

本规则适用于所有需要调查、修改、验证或审查 GrandUMI 项目的明确任务。固定快捷指令、纯对话、简单状态查询和无需检查项目内容即可回答的说明不创建执行代理。

1. 收到任务后，主代理只能先进行分类所需的最小只读检查；在完成档位判断前，不得开始实质修改、测试或部署。
2. 主代理必须在派发执行代理时的简短进度更新中说明所选档位、核心理由、实际模型名称和推理强度，然后把实际工作交给该档位对应的单个执行代理；发生升级或路由降级时，也必须同步说明新的实际模型名称和推理强度。除非用户明确要求并行或任务明确适合 Ultra，不得仅为模型路由创建多个并行代理。
3. 四档分类与配置如下：
   - **快速档 `umi_fast`**：文案、文件定位、只读查询、明确且机械的单文件调整，不涉及业务状态、规则判断或跨层联动。使用 `gpt-5.6-terra`、`medium`。
   - **标准档 `umi_standard`**：根因和影响范围已较明确的局部 Bug、普通 UI 调整、单模块功能修改。使用 `gpt-5.6-sol`、`high`。
   - **深度档 `umi_deep`**：卡牌规则与效果、对局状态机、前后端联动、数据迁移、复杂移动端交互、未知根因或多模块修改。使用 `gpt-5.6-sol`、`xhigh`。
   - **关键档 `umi_critical`**：并发与原子性、同时触发与代替效果时序、断线或服务恢复、排位结算、数据一致性或丢失风险、安全问题、部署与流量切换事故。使用 `gpt-5.6-sol`、`max`。
4. 同时命中多个档位时选择最高档；无法可靠判断时向上选择一档，不得为了速度降档。
5. 优先使用 `.codex/agents/` 中对应的项目自定义代理。若当前代理工具不暴露自定义代理类型，则必须使用显式的 `model` 和 `reasoning_effort` 参数创建同档执行代理；使用模型覆盖时采用有限上下文分叉，并在任务消息中完整传递目标、授权边界、项目约束和成功标准。
6. 执行代理发现任务实际超过自身档位时，应停止实质修改并向主代理说明升级理由；主代理重新分类后再派发更高档代理。
7. 主代理负责核对执行代理的结果、任务文件边界和验证证据，并负责最终提交、推送与测试服部署。执行代理不得自行提交、推送或部署，除非主代理在派发任务时明确授权这些最终步骤。
8. 如果当前环境无法创建指定模型的执行代理，主代理必须先如实说明路由降级；仅当当前模型与所需档位相同或更高时才可继续，否则暂停并请求用户决定。

## 本地临时文件固定使用 E 盘

本规则适用于所有 GrandUMI 本地部署、测试、验证和打包操作。

1. 部署副本、临时克隆、临时 worktree、验证目录、Git bundle、压缩测试包和其他可再生成的大体积临时产物，必须放在 `E:\GrandUMI-Temp\` 的对应子目录中，不得放在 C 盘。
2. 不得使用 `[IO.Path]::GetTempPath()`、`$env:TEMP`、`$env:TMP` 或其他可能解析到 C 盘的路径承载上述 GrandUMI 临时产物。
3. 脚本必须通过 `ops/windows/GrandUmiTemp.ps1` 获取临时目录；E 盘不存在、不可写或空间不足时应立即停止并报告，不得回退到 C 盘。
4. 任务结束后应清理自身创建的临时产物；需要暂时保留以便排查时，也只能保留在 `E:\GrandUMI-Temp\` 下。

## 移动端竖屏 UI 适配

本规则适用于仓库内所有新增或修改的前端 UI，不限于对局页。

1. 每个新 UI 和每次 UI 调整都必须同时考虑电脑端与手机竖屏，不得在功能完成后再补做移动端适配。
2. 至少验证 `390×844` 和 `360×780` 两档手机竖屏；对局页还必须验证“竖屏设备自动旋转横屏画布”的实际显示结果。
3. 固定、吸附或悬浮控件必须使用项目布局层提供的安全区变量，兼容刘海、圆角、Home Indicator 和移动浏览器动态工具栏，不得只用可能把控件推到可视区外的硬编码边距。
4. 主要操作按钮在所有验证尺寸下必须完整可见、可点击，且触控区域不得小于 `44×44px`；同时检查其与聊天、观战、设置、菜单等常驻控件是否重叠。
5. UI 改动应补充与风险相匹配的自动化回归测试，并在交付前进行实际浏览器尺寸检查。

## 完成后的提交、推送与测试服部署

本规则适用于整个仓库。已经实现并通过相应验证的 Bug 修复、功能修复、功能补齐、新功能和新需求，完成后应自动提交并推送远端，然后自动运行 `deploy-test.ps1` 部署测试服，无需用户在每个任务中重复授权。该预先授权仅覆盖测试服；不得自动部署、检查或汇报候选服及正式服，也不得调用 `deploy-new-hk-candidate.ps1`。

1. 自动提交和推送前，必须先完成与改动对应的验证，并按照“修复日志缓存”规则在 `changelog-cache/pending/` 中新增记录。仅有方案、仍在调查、尚未实现或验证失败的工作不得提交或推送。
2. 必须核对本次任务的文件边界，只暂存并提交当前任务涉及的代码、测试、配置、文档和更新日志记录，不得使用 `git add -A`、`git add .` 等方式将无关改动混入提交。
3. Git 提交信息应按改动性质使用 `feat: 简要说明`、`fix: 简要说明` 等清晰格式。提交完成后自动推送当前分支；推送成功后自动运行 `deploy-test.ps1`。
4. 本节是对后续功能与 Bug 改动所需 Git 提交、推送和测试服部署操作的预先授权，满足上述条件后无需再次请求用户确认。该授权不包含候选环境或正式服发布批准，不得自动运行 `approve-release.ps1` 或以其他方式批准、部署正式服。
5. 自动部署测试服时，只有 `deploy-test.ps1` 执行成功且测试服外网 HTTP 验证通过，才可说明已经部署；任一阶段失败时必须如实报告失败环节。
6. 如果当前不在 `main` 分支、相关测试失败、任务文件与无关改动无法安全区分、远端分支存在冲突，或其他前置条件不满足，应停止自动提交、推送或部署并向用户报告，不得擅自暂存、覆盖、丢弃或夹带其他人的改动。
7. `changelog-cache/pending/` 中保留待正式发布的记录，不妨碍已完成改动日常自动提交和推送；正式发布前仍须按下方规则汇总并归档全部相关记录。

## 修复日志缓存

本规则适用于整个仓库，并与各子目录中的补充规范共同生效。

1. 任何 Bug 修复、功能修复或功能补齐，只有在实现完成并通过相应验证后，才可标记为完成。
2. 每项已完成的修复必须立即在 `changelog-cache/pending/` 中新增一份 Markdown 记录，不得等到发布时再凭记忆补写。
3. 一项修复对应一个文件，文件名使用 `YYYY-MM-DD-简短英文标识.md`。记录必须包含日期、分类、影响范围、玩家可见说明、技术说明和验证结果。
4. 玩家可见说明应描述玩家能感知的变化，避免直接堆砌类名、方法名或内部实现细节；技术说明用于开发追溯。
5. 未完成、未验证、仅有方案或仍在调查中的工作，不得写成已完成的更新记录。
6. 准备发布时，必须读取 `changelog-cache/pending/` 下的全部记录，按“新增 / 修复 / 优化”分类汇总到 `opcgpro-web/src/data/changelog.ts` 的最新版本条目中，并使用新的唯一 `id`、版本号、日期和标题。
7. 确认待发布记录已经全部进入更新日志后，将原始记录移动到 `changelog-cache/published/<版本号>/`。不得静默删除、遗漏或只归档而不发布。
8. `changelog-cache/pending/` 仍有修复记录时，发布准备视为未完成，不得批准正式发布。

记录格式、示例和完整发布检查清单见 `changelog-cache/README.md`。

## 共享账号正式发布与跨环境迁移（长期规则）

本规则用于后续包含共享账号功能的正式服发布。正式服唯一域名始终为 `ygo.grand-umi.com`。

1. 截至 2026-08-27，测试服已运行包含共享账号功能的提交 `1f7215ff`；正式服仍为不支持该功能的旧版本。因此测试服当前仍回退使用本地账号库，任何人不得宣称正式服与测试服账号已经互通。
2. 下次正式服发布包含共享账号功能时，必须将共享账号的一次性迁移与权威库激活作为同一个发布步骤完成；不得只部署代码而遗漏迁移或激活。
3. 迁移时以正式数据为权威：测试服只可补入正式库中不存在且不冲突的账号，不得覆盖、改写或冲突合并正式账号。正式服和测试服后端必须同时停写，随后创建快照，并校验 `accounts.db`、`prepared`、`active` 的状态与内容。执行 `commit-authority` 后只能前向恢复，严禁回滚到旧本地账号库。
4. 只有在 `active` 已生效后，才可宣称互通完成。发布后必须实际验证正式服与测试服均解析 `/data/grandumi-shared/accounts.db`；还必须在测试管理面板用 QQ `1290973956` 反查到正式账号，并验证该账号的登录与绑定在两端互通。
5. 本规则仅保留发布前提与验证要求，绝不构成正式服操作授权。任何正式服迁移、激活、发布或部署，仍必须由用户在当次会话中明确授权；不得因本记忆自动执行。

---
卡号: OP11-040 / OP13-002 / OP14-041（E 类领袖，三张全部实现）
日期: 2026-06-19
对应反馈: #49 OP11-040、#50 OP13-002、#80 OP14-041（6-17 日报）
编译: dotnet build 0 警告 0 错误；dotnet test 59/59 通过
---

## 背景：06-17「三张均需引擎大改、不宜深夜盲改」的结论已过时

`修复记录\2026-06-17_E类领袖诊断_三张均需引擎扩展.md` 判定这三张需引擎核心扩展。
但此后 OP01-04 收尾批补过引擎机制，本次核实**当前代码实际状态**发现：

- `OnDamageToLeader` **早已派发**（`Game/LifeRevealManager.cs:99`，payload 带 `defenderOwner`=受伤方），多张卡在用。
- 任意角色被KO的 `OnAnyCharKOd` watcher **早已存在**（`Game/Phase/BattleEngine.cs:219` 战斗KO、`Effects/AtomicOps.cs:82/142` 效果KO 都派发，payload 带 `cardId`/`owner`/`reason`）。被KO卡此刻仍在废弃区、`Info`（原本力量/特征）是静态印刷数据可直接读 —— 06-17「OnCharLeaveField 无法读被KO卡 Info」的判断不成立。
- 仅 `OnTurnStart` 确实**从未派发**（`卡牌数据/` 无任何卡 effectTags 含它，OP07-097 的监听是死代码）。

机制关键：`EffectRuntime.HasEffectForTrigger` = `Array.IndexOf(card.Info.EffectTags, trigger名) >= 0`。
卡要被某事件收集触发，其 **`卡牌数据/*.json` 的 effectTags 必须含该 trigger 枚举名**。

## #49 OP11-040 路飞 —— 引擎补派发 OnTurnStart（DSL 早已就绪）

- DSL 早写在 `Effects/Definitions/_GAP_wf.json`（OnTurnStart + if isMyTurn+selfDonTotalGte:8 + LookTopReveal count5 max1 restTo:bottom match草帽一伙），只差引擎从不派发 OnTurnStart。
- **引擎改动**：`Game/Phase/TurnEngine.cs` 把 `AdvanceTurn` 拆为 `AdvanceTurnToReset`（结束阶段→切换→Reset）和 `AdvanceTurnToMain`（Draw→Don→Main）两段；旧 `AdvanceTurn` 保留为两段包装（兼容测试等其它调用方）。
- `Game/GameEngine.cs:EndTurnAsync` 在 `AdvanceTurnToReset` 之后、`AdvanceTurnToMain` 之前异步派发 `OnTurnStart`（payload owner=新回合方）。**时点精确**：此刻在 Reset 之后、抽牌/加咚之前，费用区咚数=进入本回合的咚总数（本回合 Don 尚未加咚），符合官方「我方回合开始时、场上咚≥8」判定。
- `卡牌数据/OP11.json` OP11-040 effectTags 由 `[]` 加 `["OnTurnStart"]`。
- 风险控制：派发前确认 `卡牌数据/` 全库无任何卡 effectTags 含 OnTurnStart → 唯一被收集的就是 OP11-040，零回归（59 测试全过印证）。
- 简化点：「其余自选放卡组顶或底」DSL 以 restTo:bottom（统一回底）近似，与库内其它 LookTopReveal 一致。

## #50 OP13-002 艾斯 —— 能力2（脚本，引擎钩子已具备）

- `Effects/Scripted/OP13_002_Ace.cs` 新增能力2：监听 `OnDamageToLeader`（判 defenderOwner==自己=我方领袖受伤）+ `OnAnyCharKOd`（判 owner==自己且被KO卡 `Info.Power>=6000`），【咚!!×1】=领袖被赋予咚≥1，【每回合1次】两路触发共享一个 once key（一回合只抽1次），满足则抽1。能力1（对方攻击时弃牌减力）保留不动。
- `卡牌数据/OP13.json` OP13-002 effectTags 加 `OnDamageToLeader`、`OnAnyCharKOd`。

## #80 OP14-041 汉库克 —— 第二段（脚本，引擎钩子已具备）

- `Effects/Scripted/OP14_041_BoaHancock.cs` 新增第二段：监听 `OnAnyCharKOd`（判 owner==自己、被KO卡 `Info.Power>=5000` 且 `HasKeyword("亚马逊·百合")||HasKeyword("九蛇海盗团")`），【咚!!×1】+【每回合1次】，满足则我方可选将对方生命顶最多1张加入对方手牌。第一段（对方回合我方角色登场抽1）保留。
- `卡牌数据/OP14.json` OP14-041 effectTags 加 `OnAnyCharKOd`。
- 特征字符串核对：卡库为「亚马逊·百合」（带中点·）、「九蛇海盗团」。

## 注意（实机验证点）

OnTurnStart 系首次派发，建议实机验证：回合开始时 OP11-040 在咚≥8 时弹检索面板；OP13-002 贴咚后受伤/6000角色被KO抽1（每回合1次）；OP14-041 贴咚后我方5000+九蛇/百合角色被KO时弹「对方生命入手」确认。

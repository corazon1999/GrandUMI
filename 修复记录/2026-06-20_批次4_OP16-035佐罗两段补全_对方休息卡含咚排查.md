# 2026-06-20 批次4：OP16-035 佐罗补全 +「将对方卡牌转休息」目标含咚排查

## 反馈（OP16-035 罗罗诺亚·佐罗）
1. 「将对方最多1张**卡牌**转为休息状态」——活跃咚、对方领袖、舞台都应可选，原实现只列对方领袖+角色。
2. 「之后，可以丢弃我方1张手牌。那样做则赋予我方领袖最多3张休息状态的咚!!」——**第二段整个没实现**（原 DSL `_matcher` 明写"忽略...因 triggers 不支持 cost"），所以没提示。

## 通用机制（新增）
- 引擎 `AtomicOps.PromptRestOpponentCards(ctx, n)`：让玩家从**对方**活跃的 领袖/角色/舞台/咚!! 中选最多 n 张休置（min 0，可不选）；四类同列（卡牌走卡图、咚走 donChoices token）；卡牌走 RestCard、咚直接置休息。
- DSL op `RestOpponentCards`（`{op:"RestOpponentCards","n":N}`）→ 调上面助手。
- 客户端无需新改：`PromptOverlay` 通用分支已能渲染 donChoices 咚 token（批次3 OP16-033 时加的 donChoiceMap），"RestOpponentCardsOrDon" kind 直接复用。

## OP16-035 改为脚本卡（OP16_035_Zoro.cs，原 DSL 条目已删，脚本优先于 DSL）
- 第1段：`PromptRestOpponentCards(ctx,1)`。
- 第2段：`ConfirmOptional` 弃1手牌 → `DiscardOwnChosen` 选弃 → `AttachDonFromCost(me, leaderId, 3, DonState.Rest)`
  （"赋予领袖休息咚"= 从费用区取休息咚附给领袖，已核 EB02-006/EB02-011/OP11-095 同款原文均如此实现）。

## 排查「将对方N张卡牌转休息」(泛指,应含对方 领袖/角色/舞台/咚) 同类卡 → 已修
- **OP13-033 弗兰奇**（脚本，KO时休对方2张）：原只对方角色 → `PromptRestOpponentCards(ctx,2)`。
- **OP14-024 锦卫门**（DSL，KO时休对方1张）：原 Choose 对方领袖+角色 → `RestOpponentCards n=1`。
- **OP15-032 布鲁克**（DSL）：**登场时"将对方1张卡牌转休息"整段缺失** → 补 `triggers: OnEnterField → RestOpponentCards n=1`。

## 排除（"角色"专指，非泛指卡牌，现行正确）
OP10-100 闪电、OP10-110 火特&瓦亚、OP14-038、OP15-102 甘·福尔、ST29-002 撒谎布（均"将对方角色转休息"）。

## 排查发现的【其它缺口】——已顺手补掉
- **OP15-032 布鲁克【启动主要】**：补全为 `cost:{selfToTrash}` + `if:{leaderHasKeyword:草帽一伙}` + Choose `filter:{originalCostLte:8}` → Activate。
- **OP14-024 锦卫门【登场时】**：ActiveOwnDon:3 之后补 `{op:"NoPlayCharacter"}`（本回合无法登场角色）。
- 新增 DSL op `NoPlayCharacter`（= `NoPlayCharacterThisTurn.Add(owner)`，回合末自动清）。

## 上线
本批 + 之前累积各批已一键更新部署到 grand-umi.com（三服务 Running、首页 HTTP 200）。

## 验证
- 服务端 `dotnet build` 0 错 0 警告；`dotnet test` 59/59；客户端 `tsc` 0 错。
- 本批客户端无改动（复用批次3 的 donChoices 混渲）。

## 待实机复测
OP16-035 登场：对方有活跃咚/舞台时选择列表是否齐全；弃手牌后领袖是否+3休息咚（+3000力量）。OP13-033/OP14-024/OP15-032 同验。

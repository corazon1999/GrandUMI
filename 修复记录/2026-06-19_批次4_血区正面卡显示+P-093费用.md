# 2026-06-19 批次4 前端显示 + 数据修正

## #116 正面朝上放置到血区的卡牌未正常显示
根因（核实实际文件）：后端**已下发** `lifeFaceUp`（StateSnapshotBuilder.cs:113，每张生命牌 {faceUp, number}，
faceUp 时 number 为公开番号），但前端断链——`PlayerView` 类型无此字段、`LifeArea.tsx` 只硬编码画卡背。
（注：调查 agent 提到的 net.ts 不存在，类型实际在 store/gameStore.ts。）

修复：
- `src/store/gameStore.ts` PlayerView 加可选 `lifeFaceUp?: {faceUp:boolean; number:string|null}[]`（snapshot 经 `s.my=msg.my` 直接结构赋值，字段已在数据里）。
- `src/components/game/LifeArea.tsx` 重写：遍历 lifeFaceUp，faceUp&&number → `<CardItem card={getCard(number)}>` 显示卡面、
  zIndex 提到背面牌之上以便看清；否则保持原卡背样式。后端不动。
- 校验：`npx tsc --noEmit` 0 错误。

## P-093 特拉法尔加·罗 费用应为4（#128）
核实三处 P.json（opcgpro-web/public/data、卡牌数据、卡牌数据_含原文）P-093 均为 cost "5"（应为4，官方为4费/6000/紫/斩/阻挡者）。
纯数据录入错误。三处统一改为 "4"，并重跑 `build:cards` 重建 allCards.json 单包（version=92d27ae976be）。
后端读 卡牌数据/P.json，需重启生效。

## 待确认（批次4 范围外，未改码）
- #115「没有0费事件卡」：核实非费用筛选bug——0费事件多为 subscript=1，被"角标1默认隐藏"过滤（设计行为），
  非角标1的0费事件默认可见。需用户指认具体看不到的卡号再定。
- #100 P-107：脚本逻辑经核正确（非代码bug），疑实测咚未真达10，待实机确认，未改码。
- #127「攻击时咚×1」全量审计：75张独立工程，单列后续批次。

# 2026-06-19 批次3 单卡 C# 脚本修复（簇A 咚×1门槛 + 特定卡 + 领袖）

逐张先核实卡面原文 + 实际文件状态（多张被调查 agent 误判，已纠正）。

## 引擎小改
- `Game/Phase/TurnEngine.cs` EnterEndPhase：回合末新增清理**手牌**卡的 `CostModThisTurn`
  （原只清场上角色+领袖；OP16-087 给手牌"光月桃之助"+20 需回合末清，否则跨回合残留）。
- 批次1 的入队/排空扩展 `from` 来源区（"hand"/"trash"/"deck"/"life"），写进 OnAllyCharEnter payload，
  供 OP16-079 区分"从废弃区登场"。（AtomicOps 四个 Play*Free + GameState.PendingEnterField.From + EffectRuntime.ResolveEnterField）

## 逐卡
### OP16-034 蒙奇·D·路飞（#69/#106）——已存在脚本，无需新写
`Scripted/OP16_034_Luffy.cs` **早已实现**（30阈值持续效果表达"每异名角色+1000" + 委托 DSL 跑登场检索）。
调查 agent 称"无脚本"系误判。effectTags=["OnEnterField"] 正确。→ 应已可用，待实机复测。

### OP16-054 Mr.1（#107，dup #122）——新建 OP16_054_DazBones.cs
【咚×1】【我方回合】手牌≥5则+3000：注册固定 ContinuousEffect(PowerDelta=3000)，门槛写进 Predicate(仅自身)；
登场抽1委托 DSL。模板 OP16_017。

### OP16-055 本萨姆（#109，dup #121）——新建 OP16_055_Bentham.cs
确认是【咚×1】【攻击时】（#109"被赋予咚"与#121"攻击时"是同一效果）。
OnEnterField 抽1；OnAttackDeclare 门槛咚≥1 → `OriginalPowerOverride = 对方领袖当前力量`（本回合性，回合末清；咚加成仍叠加其上）。

### OP16-041 巴奇（#125）——重写 OP16_041_Buggy.cs
旧占位双错（HandlesTrigger=OnKO 与 effectTags=OnCharLeaveField 不一致致永不触发 + 缺咚门槛）。
重写监听 OnAnyCharKOd（战斗/效果KO）+ OnCharLeaveField（非KO离场），覆盖全部离场；咚×1门槛+每回合1次；
离场卡含《因佩尔地狱》才发动，从手牌登场 OP16-042。**effectTags 补 OnAnyCharKOd**（卡牌数据/OP16.json）。模板 OP14_041。

### OP16-087 阿忍（#102，dup #118）——新建 OP16_087_Onin.cs
可选自送废弃→领袖《和之国》则抽1 + 选我方1张"光月桃之助"(场上优先/含手牌) `CostModThisTurn+=20`。
+20 用于启用 OP16-084 桃之助【启动主要】(需费用≥20)的连招——核 OP16-084 脚本确认 +20 有意非笔误。

### OP16-079 大和领袖（#119）——重写 OP16_079_Yamato.cs
旧为 no-op 占位。新监听 OnAllyCharEnter：仅登场方=我方 且 from=="trash" 且登场卡含《和之国》→ 赋本回合【速攻】。
依赖批次1：PlayFromTrashFree 现在派发 OnAllyCharEnter(from="trash")，且被登场角色 TurnPlayed=本回合本不能攻击，获速攻后可攻。

### ST22-015 因为我是白胡子!!（#101 前半）——新建 ST22_015_WhitebeardEvent.cs
领袖含〈白胡子海盗团〉→登场手牌≤1张"爱德华·纽哥特"（其登场时由批次1兜底触发，即 #101 后半的 OP13-042）；
之后可选生命顶/底1张入手，那样做则领袖+2000直到下个对方回合结束（P-107 式 TurnCount 限期持续效果，源挂领袖避免被"源离场"清理）。
DSL 无"可选后接条件加成"门控，故用脚本。

## 验证
dotnet build 0 错误；dotnet test 59/59 通过。
回归：测试 OP16_041 原断言用 OnKO 触发失败 → 修复后改用 OnAnyCharKOd+payload+咚门槛 重写测试（重命名 OnImpelDownLeave）。
另：修复了 PromptSystem.cs 中阻塞编译的命名空间笔误（GrandUMI.Cards.CardInstance → CardInstance，CardInstance 实在 GrandUMI.Game；用户未提交 WIP）。

### OP16-080 马歇尔·D·提奇领袖（#113，补漏）——修 OP16_080_Teach.cs
持续【对方回合中】我方角色费用+1 的 ContinuousEffect Predicate 只排除领袖、未限定"在场上"，
而 HandPlayCost 也走 ContinuousCostBonus → 手牌角色费用被误+1。
修：Predicate 加 `s.Players[owner].Characters.Any(ch => ch.Id == c.Id)` 限定仅场上角色。

## 待实机复测
OP16-034/116 经核实现已正确（agent 误判为缺失），需实机确认；其余新写脚本需对战验证交互。

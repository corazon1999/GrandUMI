---
范围: 核实判定为「误报/卡号错」与「需后续/需数据」的反馈
日期: 2026-06-19
对应反馈: #94、#95、#67（误报/卡号错）；#61、#55（缺口）
---

## 误报（代码正确，待实机复测，已 mark wontfix）

### #94 OP03-086 斯巴达姆 —— 误报
- `卡牌数据/OP03.json` OP03-086 effectTags=`["OnEnterField"]` 正确；`Effects/Scripted/OP03_086_Spandam.cs` 完整实现【登场时】：我方领袖特征含 CP 时确认顶3张、公开≤1张含CP且非自身名加手牌、余放废弃。与卡面一致。
- 「不发动」的真因：领袖特征不含 CP 时按卡面就是不发动（脚本用 `Keywords.Any(k=>k.Contains("CP"))` 直接读领袖特征，**不走单人测试通配**）。实机请用 CP 系领袖复测。

### #95 OP08-084 杰克 —— 误报
- DSL（`Effects/Definitions/OP08_wf.json`）：cost=restSelf，then=`[Draw n1, DiscardOwnChosen n1, Choose OpponentCharacterCostLe3 min0, KO]`，**Draw 在 then 首位**。
- 解释器 `RunSteps`（`DslInterpreter.cs`）逐个 await，唯一提前 return 是 `IsGameOver`；各 op 内部 `if target is null break` 只跳出自身局部块，不影响后续、更不回退已执行的 Draw。故「抽1」必第一个无条件执行，「只执行休息/丢牌/KO 而不抽1」在代码上不可能成立——疑观感（抽1后立即弹弃1框，未注意手牌已+1）。
- 附注（与本反馈无关的已知缺口）：卡面「此角色费用+4」静态修正 DSL 未实现。

### #67 OP09-099 —— 卡号误报（非本卡问题）
- OP09-099 是**舞台卡**（power「-」），唯一效果是【启动主要】丢1手牌+横置本舞台→确认顶3张公开≤1张《黑胡子海盗团》入手余放底；DSL（`Definitions/OP09.json`）与 effectTags=`["ActivatedMain"]` 均正确。本卡**没有任何力量/攻击力叠加效果**，反馈「攻击力叠加没发动」无对应物。
- 极可能是卡号记串——「攻击力叠加」正是 **#69 OP16-034**（咚×1每异名+1000，确已未生效，本轮已修）。OP09-099 不改。

## 缺口（本轮未做，需后续/需数据）

### #61 检索面板部分卡图空白 —— 已定位，根治需后端审计（本轮未做）
- 现状：带 `extra.choiceCards` 下发的 prompt 卡图均正常（扫描真实对局日志 blanks=0）。空白只出现在两类：① 个别**脚本卡**选择隐藏区（卡组/废弃/生命）时未下发 choiceCards → 前端 `findCardById` 反查不到 → 显示占位（与已修 #19 同类）；② 首屏 `loadAllCards` 未完成时弹检索的时序竞态（低频）。
- 前端无法彻底兜底（隐藏区镜像 `trashNumbers` 是纯番号无 id，按 id 反查不到）。根治需后端对所有「选隐藏区卡且未传 choiceCards」的 `ChooseCards` 调用补 `choiceCards`——属跨多脚本的审计任务，本轮未做以免半吊子修复引入新问题。建议作为独立任务推进。

### #55 ST15 卡表不全 —— 需官方数据（本轮未做）
- `opcgpro-web/public/data/ST15.json` 仅 2 张（ST15-001 阿特摩斯、ST15-002 白胡子），且 set 字段为「历代典藏合集 PRBC-02」、image 带 `_02`——是**异画/复刻收录**，并非完整起始卡组。缺领航与主卡组其余卡。
- 补全需准确的 ST15 官方完整卡表（领航+全部主卡组卡）。**不能凭记忆编造卡号/费用/颜色/效果**（会污染卡库、影响对战合法性校验）。已向用户请示提供官方卡表后再补；补后需跑 `build:cards` 重新生成 allCards.json、并按需补卡图与卡效。

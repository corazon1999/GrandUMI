# 2026-07-09 前端批量修复（GrandUMI 群 297542853 剩余反馈）

本轮处理群 297542853 剩余 open 反馈。beast 群（1039789967）13 条全部跳过（隔壁项目，规则引擎不在本仓库）。

## 已修复（代码改动）

### #187 休息咚换色（选咚弹窗）
- 现象：用效果把咚放回咚卡组的**选咚弹窗**里，休息咚仍是黄色、只靠旋转区分，难辨。
- 之前工作区只改了主牌桌 `DonCardItem.tsx`（未覆盖弹窗）。
- 改 `components/game/PromptOverlay.tsx` 选咚 token：按 `d.state` 分色——休息=靛蓝(indigo)、附着=琥珀、活跃=黄，标签色随之。与 DonCardItem 主牌桌靛蓝统一。

### #162 手机卡片大图过大 + 登录页视口
- `components/ui/CardZoomOverlay.tsx`：大卡图高度由 `min(78vh,640px)` 改为 `min(78vh,640px,calc(88vw/0.717))`，同时按 88vw 约束，防竖屏窄屏派生宽度超屏横向溢出。
- `components/home/LoginPanel.tsx`：`h-screen`→`h-[100dvh]`（修 iOS 视口），登录卡片加 `max-w-[90vw]`。

### #188 手牌数字角标
- 工作区已完整实现（`components/game/HandArea.tsx` 双方手牌区数量角标，`pointer-events-none`），本轮确认保留。

### #238 回合倒计时（仅前端显示，不判负）
- 决策：只做前端显示。后端无权威计时，超时判负本轮不做。
- `components/game/GameBoard.tsx`：新增 `TurnTimer` 徽章挂在 `PhaseTrack`，每次后端权威 `turnCount` 变化即从 300 秒重新倒数，≤30 秒转红；仅对局中（`live=!isObserver&&!isPlayback`）走秒，回放/观战不计时。

### #239 大厅已选卡组显示领袖卡面（仅自己）
- 决策：只显示自己已选卡组领航图，不改后端、不做对手领航。
- `store/netStore.ts`：`SelectedDeck` 加 `leaderSprite?`。
- `components/home/DeckChoosePanel.tsx`、`MainPanel.tsx`：两处 setGlobalDeck 补传 `leaderSprite`。
- `components/home/LobbyPanel.tsx`：已选卡组卡片加领航头像 img（onError 回退 CardBack）。

### #241 选择成功提示
- 决策：只做前端通用"选择成功"提示，不做被选目标卡特效（需后端联动）。
- `store/gameStore.ts`：加 `promptFlash` nonce + `flashPromptSuccess()`。
- `components/game/PromptOverlay.tsx`：`handleConfirm` 确认后调 `flashPromptSuccess()`。
- 新增 `components/game/PromptSuccessFlash.tsx`：居中"选择成功 ✓"浮层，约 1.1s 自动淡出，`pointer-events-none`；挂在 `app/game/page.tsx`（仅玩家视角）。

### #153 血区正面显示
- 经查 2026-06-19（批次4）已修复并提交，数据链路 `StateSnapshotBuilder→gameStore→LifeArea` 完整。本轮无需改代码，标 fixed。

## 非 bug / 待资产（未改代码）

### #229 四周年礼盒 → wontfix
- 本项目是对战模拟器，**无抽卡/礼盒/收集系统**，功能从未实现，玩家疑与官方 App 混淆。

### #210 OP07-051 无普通异画 → 保持 open，待卡图
- 非代码 bug：imageManifest 已有 4 版本（通常/SP/2周年SR/2周年平行），缺"标准平行异画"卡图**资产**。
- 需先确认该卡确有标准平行异画并取得图 → 放 `public/cards/op07/` → 追加 imageManifest → `gen-card-thumbs` → `build:cards` → `sync-assets-hk.sh`。代码零改。

### #169 回放显手牌/血区 → 保持 open，单独排期
- 回放数据源已脱敏（对手手牌/背面生命卡号未落盘），纯改 UI 无效。需后端加"全信息快照/上帝视角"回放接口 + 防作弊（仅对局结束回放语境下发）。工作量中，另立。

## 验证
- `opcgpro-web` 下 `npx tsc --noEmit` → 0 错误。

## 关联
- 记忆 [[game-page-responsive]]、[[don-return-picker]]、[[reveal-to-opponent-mechanism]]、[[card-fix-record-convention]]。
